# CLAUDE.md

このファイルは、このリポジトリで作業する Claude / コーディングエージェント向けのプロジェクト指示です。

## プロジェクトの目的

このリポジトリは、reCAPTCHA 風の画像認識タスクを題材にした、研究・教育目的の機械学習実験リポジトリです。

目的は、実サービスの CAPTCHA を突破することではなく、ローカル画像・公開データセット・自作データセットを使って、画像分類、物体検出、セグメンテーションの性能や実験手法を検証することです。

## 安全上の前提

- 実 CAPTCHA サービスの突破、自動送信、ブラウザ操作、外部サイトへの適用、認証回避を目的とするコードは追加しない。
- このリポジトリで扱うのは、ローカル画像・公開データセット・自作データセットに対する画像認識実験に限定する。
- `reCAPTCHA` という語は研究対象のタスク形式を指すもので、実サービスへの攻撃を意味しない。

## 対応したいクイズ形式

この実験では、reCAPTCHA 風の2種類のクイズに対応できるようにする。

### 1. 画像あてクイズ

3x3 の9マスにそれぞれ独立した画像が表示され、その中から指定されたお題に合う画像だけをすべて選択する形式。

- お題に合う画像をすべて選ぶ必要がある。
- お題に合わない画像を1つでも選ぶと不正解。
- お題に合う画像を1つでも選び漏らすと不正解。

### 2. マス選択クイズ

1枚の画像が 3x3 または 4x4 などのグリッドに分割され、指定されたお題の物体が含まれるマスをすべて選択する形式。

- 対象物が少しでも含まれるマスを選ぶ必要がある。
- 選び漏れや余計な選択があると不正解。
- 物体の位置情報が重要になるため、物体検出・セグメンテーションの実験対象として扱う。

## 実験ラインの扱い

このリポジトリには、画像分類によるアプローチと、物体検出・セグメンテーションによるアプローチが共存する。

- Claude は作業対象のファイル、Issue、ユーザー指示から、どちらの実験ラインかを判断する。
- 一方の方式を理由なく削除したり、もう一方に統一したりしない。
- 画像分類は「画像あてクイズ」のベースラインや比較対象として重要である。
- 物体検出・セグメンテーションは「マス選択クイズ」や、画像内の位置情報が必要なタスクで重要である。

## 実験結果・作業ログ

実験コードを変更した場合は、可能な範囲で以下を Markdown に記録する。

- 実行コマンド
- 使用データセット / 入力ディレクトリ
- モデル・重み
- 主要パラメータ
- 出力先
- 結果の要約
- 次に確認すべきこと

記録先は原則 `docs/` 配下とする。既存ドキュメントに追記できる場合は、重複ファイルを増やすより追記を優先する。

## データ・重み・生成物の扱い

大量の画像データ、学習済み重み、学習結果、生成データセットは実験資産として扱い、不用意に削除・移動・上書きしない。

特に以下は注意する。

- `img/`
- `busbus/`
- `img_bus_rain/`
- `src/my_recaptcha_dataset/`
- `src/test_images_*`
- `src/labels_*`
- `src/yolo_dataset/`
- `runs/`
- `*.pt`
- `*.pth`

クリーンアップ、再生成、ディレクトリ構造の整理、大規模なリファクタリングが必要な場合は、事前にユーザーへ確認する。

## 実行環境

- Python 実行は原則 `uv run python ...` を使う。
- 依存関係は `pyproject.toml` / `uv.lock` に従う。
- 学習・推論では GPU / MPS / CUDA の有無に依存するため、既存スクリプトの `--device auto` を優先する。
- 必要に応じて `cpu`, `mps`, `0`, `cuda:0` などを使う。
- 長時間の学習や大量データのダウンロードは、ユーザーに確認してから実行する。

## コーディング規約

- 関数には、その関数が「何のために存在するか」を説明するコメントまたは docstring を付ける。
- 実装手順をなぞるだけのコメントではなく、目的・入力・出力・前提を説明する。
- 既存コードが日本語コメント中心なので、新規コメントも原則日本語で書く。
- ライブラリ API 名、モデル名、変数名は英語のままでよい。

## リファクタリング方針

このリポジトリには実験コード、データ、生成物が散らばっているため、今後ディレクトリ構造の整理とリファクタリングを行う可能性が高い。

リファクタリング時は以下を守る。

- 実験結果やデータ資産を壊さない。
- 既存スクリプトの実行方法が変わる場合は、README または `docs/` に移行手順を残す。
- 大きな移動は小さなステップに分ける。
- 画像分類ラインと物体検出・セグメンテーションラインの両方を尊重する。
- ファイル移動後は、相対パス、出力先、ドキュメント内のコマンド例を確認する。
