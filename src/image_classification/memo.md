# 発表に向けた全体まとめ（2026-07-14時点）

発表準備用に、これまでの経緯を整理し直した。まずここだけ読めば全体が追えるようにする。週ごとの生ログはこの下にそのまま残している。

## 何を作っているか

reCAPTCHA風の「9枚の画像からバスをすべて選ぶ」クイズを、AIに解かせる実験。9枚を1枚ずつ「バスか、バスでないか」の二値分類にかける方式で、モデルはImageNetで事前学習済みのResNet18。

比べているのは大きく2つのやり方。

- zero-shot: 追加学習なしのResNet18をそのまま使う
- FT（ファインチューニング）: COCOの写真にreCAPTCHA風の劣化をかけた合成データで、少しだけ再学習する

評価には、実際のreCAPTCHA v2から集められた公開データセット（`nobodyPerfecZ/recaptchav2-29k`、100×100タイル約29,000枚）を使う。

## これまでの流れ

| 週 | 日付 | 何をしたか |
|---|---|---|
| 9週目 | 6/16 | 他メンバーの物体検出・データ収集コードをpullして整理した |
| 10週目 | 6/25 | bus/other分類を一通り実装。合成の評価データではzero-shotがFTに勝ち、経過報告で「その劣化フィルタは本物に似ているのか」と突かれた |
| 11週目 | 6/30 | 本物データセットを導入。フィルタを本物寄り（低解像度＋ぼかし＋JPEG再圧縮）に作り直し、FIDで妥当性を数字にした。本物での評価ではFTがzero-shotを逆転した |
| 12週目 | 7/7 | 進捗報告。その最中にモデル取り違え事故が発覚し、復旧とアブレーションのやり直しをした |
| 13週目 | 7/14 | コードとデータの置き場を整理して再学習。3×3デモを試したら精度が悪く、本物画像を学習にも混ぜる実験（mixed-real）の準備を始めた |

15週目が発表会。14週目はレポートと発表資料に充てる。

## 発表で言い切れること

1. **劣化フィルタの妥当性をFIDで示せた。** 新フィルタ（低解像度＋ぼかし＋JPEG）は本物busへの特徴距離165で、比べた4条件の中で本物に最も近い。旧・雨フィルタ（白線を描くだけ）は378で、劣化なし（207）より遠ざかる逆効果だった。数字と見た目の比較画像の両方で説明できる。
2. **本物データでだけFTがzero-shotに勝った。** 晴れや雨の通常写真ではzero-shotが上なのに、本物reCAPTCHAではAP 0.875 → 0.906（+0.031）。本物寄りの劣化で学習したことが本物への転移を良くした、という筋がFIDの結果とも合う。
3. **劣化つきの水増し自体は効く。** アブレーションで、劣化なしの学習はAP 0.842までzero-shotより落ちた（過学習の実例）。劣化ありは0.868〜0.911。授業で習った過学習・正則化・データ分割の話とそのまま接続できる。

## 言い方に気をつけること

- **「人間を超えた」とは言わない。** モデルのmaxF1 0.827はタイル1枚単位、人間の約81%はクイズ1問（9マス全部正解）単位で、測り方が違う。数字を並べて優劣は語れない。
- **新フィルタが旧フィルタより精度を上げるかは未決着。** やり直したアブレーションでは新0.868 < 旧0.911で、単一シードのブレが大きい。言えるのは「FIDで本物に最も近い」ことまで。
- **AP 0.906は過去の学習時点の記録。** 7/14に再学習した現行モデル（val bus F1 0.8761）のtest性能は未計測。数字を出すときは、どのモデルをいつどのデータで学習したかをセットで言う（7/7の取り違え事故の教訓）。

## デモの現状と次の一手

3×3デモ（`eval/demo_grid.py`）自体は動く。ただし7/14に試したところ誤判定が目立ち、そのまま発表で見せられる出来ではなかった。合成データだけの学習では本物とのドメイン差が埋まっていない（domain AUC≈1.0）ことと話が合う。

そこで、本物画像の一部を学習にも混ぜる案（mixed-real）に着手した。ズルにならないよう、先に全29kをtrain/val/testへ固定分割し、test 9,370枚は学習から完全に隔離した（見た目がほぼ同じ近重複画像も同じ側にまとめてある）。分割、学習コード、学習前の監査までは完了。**本学習と固定testでの比較はまだやっていない。**

## 発表までにやること

1. mixed-realモデルを本学習し、固定test 9,370枚でsynthetic-onlyモデルと比較する。
2. 改善したらデモをmixed-realモデルで撮り直す。改善しなくても「合成だけでは足りない」という結果として発表に使える。
3. デモ画像（出題と判定結果）とフィルタ比較画像を資料に貼る。
4. 資料に載せる数字ごとに、モデル、学習データ、測定日を明記する。

---

# 2026-07-14（13週目）

## 今日のテーマ

画像分類ラインを整理し直し、確かめられる事実、過去の記録、未完了の実験を分けた。3×3デモを試したところ精度が悪く、本物画像を学習に混ぜる実験（mixed-real）の準備まで進めた。

## 今日やったこと

1. 画像分類のコード、データ、モデルの置き場を `src/image_classification/` 配下に統一した。
2. `main.py` でsynthetic-onlyモデルを再学習した。
3. 3×3デモ（`eval/demo_grid.py`）を試した。誤判定が目立ち、このままでは発表に出せない → 本物画像を学習に混ぜる方針へ。
4. `real_recaptcha` 全29kをtrain / val / testへ固定分割した。近重複画像は同じsplitにまとめ、リークを防いだ。
5. mixed-real学習と固定test比較のコードを追加し、学習を伴わない監査（dry-run / audit-only）まで通した。
6. `README.md` を、0から再現する手順を軸にした構成へ書き直した。あわせてリポジトリ全体（Python 5,221行）の再現性レビューをした。

主なコミットは `f3ab98ad`、`fd9c471a`、`44e4392e`。作業ブランチは `image-classification-2`。

## 再学習したsynthetic-onlyモデル

```bash
UV_CACHE_DIR=/tmp/uv-cache uv run --frozen python src/image_classification/main.py
```

| 項目 | 結果 |
|---|---:|
| 合成train | 1,164枚（bus 444 / other 720） |
| 合成val | 291枚（bus 111 / other 180） |
| epoch | 10 |
| best epoch | 4 |
| best val bus F1 | 0.8761 |
| best val accuracy | 0.9038 |
| best val precision / recall | 0.8609 / 0.8919 |

モデルは `models/best_resnet18_bus.pth`。SHA-256は `42316984a20e3e3255a22f4c08e0d94856116c6cfcae75c711734b5d233cb27a`。

注意点が2つ。busは既存185原本、otherは300原本からの学習で、均衡した0からの再生成ではない。過去レポートの本物 `AP=0.906`、最大`F1=0.827` は別の学習時点の記録で、今日のモデルのreal test性能ではない。

## 3×3デモの結果

`demo_grid.py` を何度か回したが、誤判定が目立った。何問中何問という記録は残していないので、次に回すときはseedと出力画像を保存する。

見立て: フィルタ妥当性評価でdomain AUCがほぼ1.0（本物と合成をほぼ完全に区別できる）だったことから、合成データだけの学習では本物への汎化が足りない。これがmixed-real実験に進む動機になった。

## mixed-real実験の固定split

| split | bus | nonbus | 用途 |
|---|---:|---:|---|
| train | 1,339 | 1,339 | 学習へ追加する候補 |
| val | 669 | 669 | best epochの選択 |
| test | 4,685 | 4,685 | 最終比較専用 |

- 全13,386枚、近重複グループ13,251。
- SHA-256完全一致と、dHashのHamming距離4以下を同一グループにした。
- splitをまたぐグループは0、bus/nonbusをまたぐグループも0。
- mixed-realのdry-runは、合成train 1,164枚、本物train 1,200枚、本物val 600枚で成功。
- 固定test 9,370枚のaudit-onlyも成功。
- **本学習と最終比較は未実行。**

## 全体レビューで分かった問題

結果の解釈まわり:

- タイル単位のF1と、人間のクイズ単位の正答率は別の指標。`0.827 > 0.81` だけで「人間を上回った」とは言えない。
- zero-shotのbus判定は部分文字列一致で、`minibus`、`school bus`、`trolleybus` に加えて `bustard`（ノガン）と `colobus`（サル）の確率までbusへ合算している。過去のzero-shot APはラベルIDを直して再計測が必要。
- FIDは標準のInceptionV3ではなくResNet18特徴による相対距離。他実装のFIDとは比較しない。

再現性まわり:

- `main.py` は対象ファイルが1枚でもあると工程をskipするので、部分生成状態を完了と誤認する。`--force` も既存train / valを空にしない。
- fresh cloneにはGit管理外の `data/busbus/` が無いため、`main.py` はstep 6まで完走できない。
- 固定manifestの通常監査はパスと構造の確認だけで、画像を再ハッシュして内容の変化までは検出しない。
- `uv lock --check` が不一致で終了した。`pyproject.toml` の依存が `uv.lock` に反映されていない。

他の実験ライン:

- `src/object_detection/test_best_model.py` は別ユーザーの絶対パスを参照していて、そのままでは動かない。
- 物体検出とセグメンテーションに、リファクタリング前後のパスが混在している。
- fog / mosaicの同一原本バリエーションを画像単位でランダム分割していて、train / valリークが起こり得る。
- `containers/re-recaptcha.def` は `pyproject.toml` と同じ依存集合を構築しておらず、画像分類の全工程を再現するコンテナになっていない。

## 今日実行した検証

```bash
uv run python -m compileall -q src
uv run --frozen python src/image_classification/eval/prepare_real_recaptcha_split.py
uv run --frozen python src/image_classification/train_real_recaptcha.py --dry-run
uv run --frozen python src/image_classification/eval/compare_real_recaptcha_training.py --audit-only
uv lock --check
git diff --check
```

lock同期確認だけ失敗（lock更新が必要）。それ以外は成功。

## 次にやること

1. mixed-realモデルを本学習し、固定test全9,370枚でbaselineと比較する。改善したらデモを撮り直す。
2. zero-shotのbusラベル対応をID指定へ直し、過去評価を再計測する。
3. `main.py` に期待枚数と3バリアントの完全性検査を追加する。
4. baselineモデルにもmanifest hash、データ件数、seed、best epochをmetadataとして保存する。
5. 改善が見えた場合は複数seedと信頼区間で追試する。
6. 物体検出・セグメンテーションのパスと、原本単位splitを修正する。

ローカル画像とモデル重みはGit管理外なので、数値を出す前にファイルのハッシュ、学習条件、対応データを必ず確認する。

---

# 2026-07-07（12週目）

進捗報告の日。報告書 `進捗報告_2026-07-07.md` を作成し、指定見出し（2-1 使用モデル／2-2 目標判別精度＋フィルタ妥当性／2-3 データ分離／2-4 課題点）で最終版に整えた。
今日は「①報告書作成 ②発表前の不備修正 ③その最中に見つけたモデル取り違えの発覚と修正 ④アブレーションのやり直し」を回した。以下テーマ別に整理。

## 授業資料との対応（発表で「基礎の応用」を示す材料）

授業＝知能情報実験3 データマイニング班（https://ie.u-ryukyu.ac.jp/~tnal/2026/info3/dm/）。
シラバスは回帰・過学習・正則化・データ分割・前処理・分類の基礎中心（深層学習/評価指標は明示なし＝調整中）。
「授業で習う基礎が、プロジェクトのどこで効いているか」で対応させる。

| 授業の概念 | プロジェクトでの対応 |
|---|---|
| 過学習（Ridge回帰で対策） | 劣化なしFTが本物AP 0.842まで落ちる＝過学習の実例。元画像少・数epochで過学習 |
| 正則化（alpha調整） | 劣化フィルタでの水増し＝ノイズ注入による正則化。汎化を上げる |
| train/val分割・valで調整 | ID単位で分割。bus F1ベストのepochをvalで選ぶ |
| 前処理・正規化 | 224×224リサイズ＋ImageNet平均/標準偏差で正規化 |
| 分類（決定木など） | ResNetによる bus/other 2値分類（授業は決定木、こちらはCNN） |
| 勾配降下法 | 学習はAdam（勾配降下法の改良版）で重み更新 |
| 次元の呪い・次元削減 | FIDは画像を512次元特徴に落として距離を測る |
| 可視化（matplotlib） | PR曲線・フィルタ比較・本物タイルを図示 |

授業を超えている（自分で足した発展）＝発表でアピールできる部分：
- 転移学習／事前学習（ImageNet ResNet）… 授業はゼロから学習中心
- 分類の評価指標（precision/recall/F1/AP/PR曲線）… 授業は回帰の評価中心
- FID・ブートストラップ信頼区間 … 授業範囲外の定量評価

→ 発表の一言：「授業で習った過学習・正則化・検証データ・前処理を、深層学習の画像分類に応用し、評価はFID/APで定量化した」。

## スケジュールと最終ゴール

- 今週＝**12週目**（本日進捗報告）。13週目で実装終了フェーズ → 14週目で本発表資料（レポート）作成 → **15週目が発表会**。
- 残り実質1〜2週。新しい大実験より **デモの完成** と **数字・再現性の整理** に寄せる。
- **最終ゴール＝3×3グリッドのデモ**。1枚の reCAPTCHA を9マスに分割→各マスを bus/other 判定して見せる。
  簡易枠組み（`split_recaptcha.py` ＋ `predict_recaptcha.py`）は既にある。これをブラッシュアップして本番っぽくする。

## 進捗報告で受けたフィードバック

- 実験は「**どの指標を使い、結果はこうだった**」を**数字で**示すこと。**再現性が大事**。
- → 対応：報告書 2-2 に指標（AP／F1／FID）と達成値（AP 0.906／最大F1 0.827／FID表）を明示、2-3 に再現性（シード42・uv.lock 固定・元画像単位分割）を明記。今後の実験もこの型で残す。

## いま言い切れること / 保留していること（発表の芯）

- 言い切れる：本物 reCAPTCHA で **FT 最大F1 = 0.827 が人間 ~81%（Searles et al., USENIX Sec 2023）を上回った**。AP は zero-shot 0.875 → FT 0.906（+0.031、ブートストラップCIで有意）。
- 言い切れる：新フィルタが本物に**見た目で最も近い**（FID 164.5 で最小）。旧・雨(白線)は逆効果(381.5)。
- **保留**：FIDの近さ＝精度向上とは示せていない（下のアブレーション参照）。進捗報告では課題点（予備・要追試）として正直に扱う。

## 今日やった不備修正（4件）

- **README用語修正**：評価データ img/img_bus_rain を「合成の」と誤記していたのを「劣化のない通常写真」に修正（本物写真なので）。
- **ブートストラップCI**：`bootstrap_ci.py` 作成。10,000回ペアド・ブートストラップ。本物 ΔAP(FT−ZS)=+0.021、95%CI [+0.015, +0.027]（0を跨がず有意）。晴れ [-0.167, 0]（跨ぐ）、雨 [-0.092, -0.017]（跨がない）。
- **README注記**：FIDがResNet18特徴の相対比較である件、real_recaptchaが1:1バランス評価である件を追記。
- **アブレーション**：`ablation.py` 作成（後述。当初バグあり→やり直し）。

## 事故：モデル取り違え（発覚→修正済み）

- このマシンの `best_resnet18_bus.pth`（6/25作成）と `dataset/`（`_night/_rain` 入り）は**旧フィルタ版のまま**だった。6/30の新フィルタ再学習は**別マシン(hibiki)**で行われ、.pth と dataset/ は git 管理外なので同期されていなかった。
- 影響：上のCIは実は旧フィルタモデルの数値（「FTがZSに勝つのは有意」という結論自体は有効）。初回アブレーションの条件C(新フィルタ)も中身は旧モデルで、「フィルタ種類の寄与は小さい」という当初結論は無効だった。
- 復旧：hibikiの300ベース版をマージして数値を正した（本物 FT AP 0.906・最大F1 0.827）。
- **教訓**：git 管理外の生成物（.pth / dataset/）はマシン間で同期されない。数値を出す前に「そのモデルはいつ・どの設定で学習したか」を mtime とサフィックスで必ず確認。ブランチは hibiki と共用なので push 前に fetch を習慣化。

## アブレーションのやり直し（ズルなし・正しい数字）

`ablation.py` を修正：条件C(新フィルタ)が旧モデルを流用していたのを、同一原本100枚から新フィルタで実学習する形に変更（`best_resnet18_bus_ablation_newfilter.pth` を新規学習）。3条件とも同一マシン・同一シードで再測定：

| 学習条件 | 本物AP | zero-shot(0.875)比 |
|---|---:|---:|
| 劣化なし（原本のみ） | 0.842 | −0.033 |
| 旧フィルタ | 0.911 | +0.036 |
| 新フィルタ | 0.868 | −0.007 |

- **新フィルタの物語は崩れた**：この回は新(0.868) < 旧(0.911)で、ZSにも届かず。過去の新フィルタは0.906〜0.911だったので、単一シードのブレが大きすぎて優劣を語れない。
- 言えるのは「劣化つき水増し自体は効きうる（劣化なし0.842より上）」まで。「新フィルタだから精度が上がる」は未証明。
- 反映済み：進捗報告2-2はFID妥当性まで＋2-4課題点1に精度検証を回す。README §6・ablation.md も正しい数字（新0.868）＋予備実験の枠に更新。

## 残タスク

デモ完成（14週目レポートまでに）:
- [ ] `predict_recaptcha.py` を本物 reCAPTCHA 画像で通す（現状 sample.jpg 前提）。
- [ ] 9マスの判定を⭕️/❌グリッドで見せる出力を整える。
- [ ] 使用モデルを現行の300ベース新フィルタFTに固定し、どのモデルか明記（取り違え防止）。
- [ ] デモの手順を README に「これを実行すればこの絵が出る」と再現手順で書く。

数値の再計測（余裕があれば）:
- [ ] 300ベース新フィルタを このマシンで再生成 or hibikiマシンで `bootstrap_ci.py` 実行 → 現行モデルのCIに差し替え。
- [ ] アブレーションを同一ベース・複数シードでやり直し、フィルタ種類の優劣に決着。

任意:
- [ ] busは効果が薄いので、消火栓・横断歩道など ImageNet外お題を1つ足せるか検討。


---
---
---
# 2026-06-30（11週目）

テーマ：**前回突かれた「劣化フィルタは妥当なのか？」に、本物データで決着をつける。**
本物 reCAPTCHA データセット（`nobodyPerfecZ/recaptchav2-29k`）を見つけて導入した。

## やったこと

1. **本物データセットを導入**（HuggingFace `nobodyPerfecZ/recaptchav2-29k`）
   - 実際の reCAPTCHA v2 から取った実画像 29,568枚・100×100・マルチラベル。**bus = ラベル列1**。
   - 目視確認 → スクールバス・低解像度・JPEGノイズあり。**雨だれ線/夜の暗さは無い** → 旧フィルタが的外れと判明。
   - `eval/download_real_recaptcha.py` でDL→ `real_recaptcha/{bus,nonbus}/` に保存（**git管理外**）。
2. **劣化フィルタを本物寄りに作り直した**（`data_augment.py`）
   - 旧 `make_night_image`/`make_rainy_noise_image`（暗く＋白線）→ 新 `make_recaptcha_like_image`（**低解像度化＋ぼかし＋彩度低下＋JPEG低品質再圧縮**）。
   - 学習データ生成（`save_augmented_variants`）は新フィルタ一本に。旧関数は比較用に残置。
   - サフィックスを `_night/_rain` → `_degraded1/_degraded2` に変更（`split_train_val.py`・`download_train_*.py` も追従）。
3. **フィルタ妥当性を FID で定量評価**（`eval/filter_validity.py`）
   - クリーン bus に各フィルタをかけ、本物 bus 6,693枚との **FID**（小=本物に近い＝妥当）を比較。

   | フィルタ | FID(↓近い) | clean比 | 判定 |
   |---|---|---|---|
   | clean | 206.7 | 基準 | — |
   | old_night | 179.2 | +27.5 | 少し近づく |
   | **old_rain（白線）** | **381.5** | **−174.8** | **逆効果** |
   | **new（低解像度＋JPEG）** | **164.5** | **+42.2** | **最も近い=妥当** |

4. **(B)-1 タスク転移を実証**（`eval/compare_models.py` に本物セットを追加）
   - 新フィルタのみで学習し直し、zero-shot vs FT を3データセットで比較。

   | 評価データ | 正例/負例 | zero-shot AP | fine-tuned AP | 差 |
   |---|---|---|---|---|
   | 晴れ (img) | 11/99 | 1.000 | 0.984 | -0.016 |
   | 雨 (img_bus_rain) | 50/52 | 0.978 | 0.946 | -0.032 |
   | **本物 (real_recaptcha)** | 6693/6693 | **0.875** | **0.911** | **+0.036** |

5. **一括実行スクリプト `main.py` を整備** … データ生成→分割→学習→本物DL→妥当性評価（`--from-step`/`--force`）。
6. **整理**：`ex_train_bus.py` を `未使用/` へ移動。`README.md` を現状に合わせてマニュアル化。

## 振り返り

- **問いに答えられた**：「フィルタは妥当か？」→ FIDで「新フィルタが本物に最も近い／旧フィルタの白線は逆効果」と**数字で**示せた。発表で一番強いポイント。
- **本物だけ FT が逆転**：合成（晴れ・雨）では zero-shot が勝つのに、本物では FT が +0.036 で上回る。「本物寄りフィルタで学習したことが転移性能を上げた」証拠で、FID評価とも整合。きれいな筋になった。
- **ハマったバグ**：フィルタ切替時に旧サフィックスのファイルが残って新生成分と混在したまま一度学習してしまった。`clean_stale_variants()`（現行サフィックス以外を実行前に自動削除）で解消し、`dataset/`・モデルを消してクリーン再生成・再学習。→ 教訓：生成物の命名を変えたら古い生成物の掃除をセットで入れる。
- **限界**：学習データが少ない（100ベース×3＝各300枚、bus val F1≈0.86）ので差は小さめ。domainAUC は被写体ギャップで≈1.0飽和（FIDの相対比較で代替）。

## 追記：学習データを300ベースに増量（発表準備）

上の(B)-1は100ベース×3＝各300枚での結果。発表に向けてベース画像を **100→300枚（各900枚）** に増やして再学習・再評価した。

- **bus val F1：0.857 → 0.9115** に大幅改善（過学習が緩和）。
- 本物 reCAPTCHA での **FT AP=0.906（zero-shot 0.875 に対し +0.031）**、maxF1=0.827（≈82.7%）。本物だけ FT が勝つ傾向は維持。
- **maxF1 0.827 は文献上の人間正答率 ~82% を上回る**（Motoyama et al. 2023 / Plesner et al. COMPSAC 2024）。発表で使える。
- 差が +0.036→+0.031 とわずかに縮んだのは、増量で zero-shot 側も含め全体が安定したため。筋（本物で FT 優位）は不変。
- フィルタ比較画像を `eval/make_filter_comparison.py` で生成（clean/old_night/old_rain/new を横並び）。FIDの数字＋見た目でフィルタ妥当性を説明できる。
- 結果ファイル（`eval/results/` の CSV・比較md・PR曲線）とREADMEの数値はこの300ベース版に更新済み。

## 次回のTODO

- [ ] **9マス本番デモを今の流れに繋ぐ**：ルートの `split_recaptcha.py`（画像を3×3に分割→`test_images/tile_*.jpg`）＋ `画像分類/predict_recaptcha.py`（FTモデルで各マスを bus/other 判定）が既にある。本物 reCAPTCHA を9分割→マスごと判定するデモとして整え、発表で「本番っぽい絵」を見せられるか試す。
- [x] 学習データを増やす（ベース画像を100→300）と FT の差がはっきりするか検証 → 上記「追記」。val F1 は改善、本物での FT 優位は維持。
- [ ] 「bus は zero-shot で十分。FTの効きを見せたいなら ImageNet に無い/弱いお題（消火栓・横断歩道・階段）」を発表で使うか検討。
- [ ] 本物セットを学習にも使う案（その場合 train/test を画像ID単位で厳密分割。評価分は混ぜない）。
- [ ] 発表資料に「データセット出典・ライセンス・倫理注意」を明記（README §8 に記載済み、流用する）。

---
---
---
# 2026-06-25（10週目）

## 今日やったこと（ざっくり）

画像分類（busを当てる側）を一通り回して、整理して、pushまでやった。

1. **学習データの偏りを直した**
   - 前は bus が昼間の素の画像だけ、other だけに夜・雨の加工版があった。
   - これだと「暗い画像＝バスじゃない」と覚えてしまい、雨のバスを取りこぼす。
   - bus にも同じ夜・雨加工を入れて、bus 300枚・other 300枚に揃えた。
   - 加工処理は `data_augment.py` にまとめて、bus と other が必ず同じ条件になるようにした。
2. **データのリークを直した**
   - 原本、夜、雨は同じ景色。バラバラに train/val に分けると同じ景色が両方に入り、val の点数が甘く出る。
   - `split_train_val.py` を「元画像ID単位で分ける」ように変更。3枚は必ず train か val のどちらか片方へ。
3. **FT（ファインチューニング）を学習した** … `train_resnet.py`。シード42固定、F1でベスト選択、クラス名も保存。
4. **本物データで zero-shot と FT を比較した** … `eval/compare_models.py`。
5. **ファイルを 画像分類/ に整理**して、論理単位で7コミット→ブランチ `image-classification-2` に push。
6. README とその PDF も作った（`画像分類/README.md` / `README.pdf`）。

## 出した結果（これは覚えておく）

| 評価データ | zero-shot AP | FT AP |
|---|---|---|
| 晴れ (img) | 1.000 | 0.953 |
| 雨 (img_bus_rain) | 0.978 | 0.930 |

- **bus は zero-shot（学習なしのResNet）の方が強い。** FT は晴れも雨も少し下がった。
- 理由の見立て：ImageNet版のResNetが元々バスを見分けられる／200枚で微調整したら過学習した／**自作の夜・雨フィルタが本物の雨に効かなかった**。

## 経過報告で突かれたところ → 次の宿題

**「そのフィルタ（夜・雨の加工）は妥当なの？」** と聞かれた。ここが次のテーマ。

何が問題かを自分の言葉で：
- 自分のフィルタは「画像を暗くする＋白い線を描く」だけ。
- 本物の reCAPTCHA の画像は、たぶん劣化の種類が違う(小さくボケて、JPEGのノイズが乗る等)。雨だれの線なんて入ってない。
- もし**学習や評価に使う劣化が本物と違う**なら、出した数字が本物のキャプチャで通用しない。だから「フィルタが本物に似ているか」を問われている。

## 次にやること（フィルタの妥当性を考える）

知識が少なくても進められる順で書く。上から順にやればいい。

1. **本物のreCAPTCHA画像を何枚か集めて、自分の目で見る。**
   - どう劣化してる？ 解像度が低い、ぼやけ、モザイク、JPEGのブロックノイズ、あたりを探す。
   - 雨だれの線は入ってる？ たぶん入ってない。←これが分かれば、自分のフィルタが的外れな証拠になる。
2. **自分のフィルタ画像と本物を並べて、違いを言葉にする。**
   - 例：「本物は全体的にボケて細部が潰れている。自分のは線が乗ってるだけで、細部ははっきり残っている」。
   - スクショを memo に貼ってもいい。見比べが一番説得力がある。
3. **本物に寄せたフィルタを作り直す（候補）。**
   - 縮小してから元のサイズに戻す（解像度を落とす）。
   - JPEG圧縮を強めにかけ直す（ブロックノイズが出る）。
   - 軽くぼかす（ガウシアンブラー）。
   - 雨だれの線は外すか弱める。
   - ※ `make rough image/rough_bus.py` が「低解像度化」をやっているので、まずそれを見て参考にする。
4. **フィルタを変えたら評価がどう動くか見る。**
   - 新しいフィルタで学習し直して `compare_models.py` をもう一度回す。
   - 数字が変わるかどうかが、フィルタの効きの証拠になる。
5. **そもそも論も持っておく。**
   - 評価は合成じゃなく**本物**でやる（今日それはやった。`img_bus_rain` が本物）。
   - 学習の水増しフィルタが本物に似てるかは、それとは別の話。混ぜないこと。

## 説明できるようにしておく言葉（自分用のかみ砕き）

- **フィルタ**：画像を加工する処理。今回は「暗くする＋白い線」。
- **データ拡張(augmentation)**：学習データを水増しする加工。本物に似てるほど学習が効く。
- **なぜフィルタの妥当性が大事か**：学習・評価に使う劣化が本物とズレてると、結果が本物で通用しない。
- **zero-shot**：学習しないで、最初から賢いモデル(ImageNet版ResNet)をそのまま使うやり方。
- **FT(ファインチューニング)**：そのモデルを自分のデータで少し学習し直すやり方。
- **AP**：モデルの強さを0〜1の1つの数字で表したもの。1.0が満点。詳しくは README の「指標の意味」。

## TODO（次回チェック）
- [ ] 本物の reCAPTCHA 画像を数枚集める
- [ ] 本物と自作フィルタを見比べて、違いをメモに書く
- [ ] フィルタを本物寄りに作り直す（縮小、JPEG、ぼかしを試す）
- [ ] 新フィルタで学習→ `compare_models.py` で再評価
- [ ] 「bus はそもそも zero-shot で十分。FTを見せたいなら ImageNet に無いお題(消火栓・横断歩道など)」という話も発表で使えるか検討

## 今の成果物の場所
- 学習：`画像分類/train_resnet.py` → `best_resnet18_bus.pth`（ルート。git管理外）
- 比較評価：`画像分類/eval/compare_models.py` → 結果は `画像分類/eval/results/`
- まとめ：`画像分類/README.md`（数値の根拠と指標の意味つき）
- ブランチ：`image-classification-2`（自分専用）

---
---
---
# 2026-06-16（9週目）
## pullで入った内容の整理

直近で他メンバーの PR #21・#22 が main にマージされ、それを pull した。

### 何が入ったか
- PR #21 `object_detection`：物体検出側。`src/OD.py`(YOLOv8)、`src/proto.py`(FTの雛形→今は `train_resnet.py` が後継)、`src/preprocess.py`(検出→タイル正解ラベル)、`src/download.py`(fiftyoneでCOCO取得)、grad-cam。
- PR #22 `hiraki/img`：`make rough image/rough_bus.py`(低解像度化でノイズ画像)、`busbus/`(バス画像約94枚)。

### 他の人の役割（当時の見立て）
- 物体検出(`OD.py`)＝マス当てチャレンジ。
- FT(`proto.py`)＝生 vs FT 比較の FT 側（今日その比較を実際にやった）。
- 前処理(`preprocess.py`)＝検出結果をタイル正解ラベルに変換。
- データ収集(`download.py`)・画像粗化(`rough_bus.py`)＝データ整備。

### 当時のTODO（今の状況）
- FTモデルの評価 … 済（今日 compare_models.py でやった）。
- データセットの乱立を整理 … 一部済（学習データは dataset/ に統一、評価は img/ と img_bus_rain/）。
- 依存に注意 … 他メンバーは ultralytics/fiftyone/opencv を使うが pyproject に未追加。動かすなら各自インストール。
